# Countries — Country Explorer

Open `country.html` in a browser to view the Country Explorer, which displays flags, population, languages and capitals.

Files:
- `country.html` — main page (open in browser)
- `ca.js` — country data

To run locally: open `country.html` in your browser.
